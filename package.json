{
  "name": "fifteen-game",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --configLoader runner",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
